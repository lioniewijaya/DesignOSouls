package game.interfaces;

/**
 * A contract for consumer instance (in other words, object/instance that can consume items).
 */
public interface Consumer {

    /**
     * Get maximum HP of a consumer.
     * @return max HP
     */
    int getMaxHp();
}
